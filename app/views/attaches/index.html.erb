<h1>Listing attaches</h1>

<table>
  <tr>
    <th>Fk</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @attaches.each do |attach| %>
  <tr>
    <td><%= attach.fk_id %></td>
    <td><%= link_to 'Show', attach %></td>
    <td><%= link_to 'Edit', edit_attach_path(attach) %></td>
    <td><%= link_to 'Destroy', attach, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Attach', new_attach_path %>
