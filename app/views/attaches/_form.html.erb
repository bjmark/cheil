<%= form_for(@attach,:url=>@path) do |f| %>
  <% if @attach.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attach.errors.count, "error") %> prohibited this attach from being saved:</h2>

      <ul>
      <% @attach.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :attach %><br />
    <%= f.file_field :attach %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
