<%= form_for(@brief_design,:url=>@action_to) do |f| %>
<% if @brief_design.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@brief_design.errors.count, "error") %> prohibited this brief_design from being saved:</h2>
  <ul>
    <% @brief_design.errors.full_messages.each do |msg| %>
    <li>
      <%= msg %>
    </li>
    <% end %>
  </ul>
</div>
<% end %>
<div class="field">
  <%= f.label :title ,'名称/说明'%>
  <br />
  <%= f.text_field :title %>
</div>
<%= f.hidden_field :brief_id %>
<div class="actions">
  <%= f.submit '保存'%>
</div>
<% end %> 