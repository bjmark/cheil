<%= form_for(@brief,:url=>@action_to) do |f| %>
  <% if @brief.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brief.errors.count, "error") %> prohibited this brief from being saved:</h2>

      <ul>
      <% @brief.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name,'名称'%><br />
    <%= f.text_area :name,:rows=>3,:cols=>40 %>
    <%=f.hidden_field :user_id%>
    <%=f.hidden_field :org_id%>
  </div>
  <div class="actions">
    <%= f.submit '保存'%>
  </div>
<% end %>
