<%
  #rpm/_nav
#brief
%>
<li class="first">
<%= link_to '返回', briefs_path %>
</li>
<li <%=raw 'class="active"' if brief_path(brief) == request.fullpath%>>
<%= link_to '需求', brief_path(brief) %>
</li>
<li>
<%if brief.send_to_cheil? and brief.status > 1%>
  <li <%=raw 'class="active"' if cheil_solution_path(brief.cheil_solution) == request.fullpath%>>
  <%= link_to '方案', cheil_solution_path(brief.cheil_solution) %>
  </li>
<%end%>


