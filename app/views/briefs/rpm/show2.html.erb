<%=render 'share/block',
  :nav=>{:partial=>'briefs/rpm/nav',
    :locals=>{:brief=>@brief,:back=>@back}},
  :title=>@title,
  :content=>{:partial=>'briefs/rpm/content',
    :locals=>{:brief=>@brief}}%>
<%=render 'attaches/brief_attaches',
  :brief=>@brief,
  :brief_attaches => @brief_attaches,
  :back => @back
%>

